export const selectCampersData = (state) => state.campers.campers
export const selectLoadingState = (state) => state.campers.loading
export const selectFilters = (state) => state.campers.filters
export const selectHasMoreState = (state) => state.campers.hasMore
export const selectPageState = (state) => state.campers.page
